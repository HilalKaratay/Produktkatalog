<UserControl x:Class="Produktkatalog.View.ProductTilesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Produktkatalog.ViewModel"
             xmlns:viewModel="clr-namespace:Produktkatalog.ViewModel"
             xmlns:view="clr-namespace:Produktkatalog.View" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    
    <UserControl.Resources>
        <DataTemplate x:Key="ProductDataTemplate" >
            <Border Margin="15" Background="WhiteSmoke" CornerRadius="10" BorderThickness="0.8" BorderBrush="Black">
               
            <Grid Height="250">    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="35"/>
                        <RowDefinition Height="80"/>
                    </Grid.RowDefinitions>
                    
                    <Button Content="X" 
                            Command="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}},Path=DataContext.OnDeleteProductCommand}" 
                            CommandParameter="{Binding ProductId}" 
                            HorizontalAlignment="Right" 
                            VerticalAlignment="Top" 
                            HorizontalContentAlignment="Center" 
                            FontSize="6"                             
                            Height="10" Width="12" Margin="8 8"/>
                    <Image Grid.Row="0" Height="100" Width="200" Source="{Binding BildURL}" />
                    <Grid Grid.Row="1" >
                        <Label Content="{Binding ProductName}" HorizontalAlignment="Center" FontSize="13"  Width="210" Padding="Auto" HorizontalContentAlignment="Center"/>
                        <Label Content="{Binding ProductDimension}" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="11" Width="190" Padding="Auto" HorizontalContentAlignment="Center" />

                    </Grid>
                    <Grid Grid.Row="2" Height="40" Width="200">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Content="{Binding ApplicationPlace}" FontSize="10" HorizontalContentAlignment="Center" Padding="Auto" />
                        <Label Grid.Column="0" Content="{Binding LumFlux}" FontSize="10" HorizontalContentAlignment="Center" VerticalAlignment="Bottom"/>
                        <Label Grid.Column="1" Content="{Binding LightYield}" FontSize="10"  HorizontalAlignment="Center"  Padding="Auto" Margin="0 1"/>
                        <Label Grid.Column="1" Content="{Binding Performance}" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0 1"/>
                    </Grid>                
                 </Grid>  
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <ListView x:Name="ProductTileListView"
                  ItemTemplate="{StaticResource ProductDataTemplate}" 
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  SelectedItem="{Binding SelectedProduct}"
                  d:ItemsSource="{d:SampleData ItemCount=5}">
            
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseDoubleClick">
                    <ei:CallMethodAction MethodName="DoubleClickMethod" TargetObject="{Binding}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</UserControl>
